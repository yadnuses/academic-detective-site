# Cloudflare 缓存查看与清除指南

---

## 一、确认部署状态

### 1. 登录 Cloudflare Dashboard
- 访问 https://dash.cloudflare.com
- 使用注册时的邮箱/账号登录

### 2. 找到对应项目
- 在左侧菜单选择 **Workers & Pages**
- 在项目列表中找到 `academic-detective-site` 或 `academic-detective.top`

### 3. 查看部署记录
- 点击项目名称进入详情页
- 查看 **Deployments**（部署）标签
- 确认最新一次部署的提交哈希是否为 `aff3880`
- 如果最新部署是旧提交（如 `cf6f35d`），说明部署没有更新

---

## 二、清除 Cloudflare 缓存

### 方法 1：整个站点缓存清除（推荐）
1. 在 Cloudflare Dashboard 左侧菜单选择 **Caching**
2. 点击 **Purge Everything**（清除所有缓存）
3. 确认操作，等待 10-30 秒

### 方法 2：按 URL 清除
1. 在 **Caching** 页面选择 **Purge by URL**
2. 输入以下 URL 逐个清除：
   - `https://www.academic-detective.top/`
   - `https://www.academic-detective.top/index.html`
   - `https://www.academic-detective.top/pre/index.html`
3. 点击 **Purge**

### 方法 3：按标签清除（如果设置了缓存标签）
1. 在 **Caching** 页面选择 **Purge by Tag**
2. 输入标签名（如 `static-assets`）
3. 点击 **Purge**

---

## 三、清除浏览器缓存

### Chrome / Edge
1. 按 `Ctrl + Shift + Delete`（Windows）或 `Cmd + Shift + Delete`（Mac）
2. 时间范围选择“全部时间”
3. 勾选“缓存的图片和文件”
4. 点击“清除数据”

### Safari
1. 按 `Cmd + Option + E` 清空缓存
2. 或进入 偏好设置 → 隐私 → 管理网站数据，移除对应站点

### 强制刷新单页
- Windows：`Ctrl + Shift + R` 或 `Ctrl + F5`
- Mac：`Cmd + Shift + R`

---

## 四、检查部署是否成功

### 1. 直接访问 pre 页面
- 打开 `https://www.academic-detective.top/pre/index.html`
- 如果 404，说明 `pre/` 目录没有被部署
- 如果能打开，说明只是首页被缓存

### 2. 查看 GitHub 提交
- 打开 https://github.com/yadnuses/academic-detective-site
- 确认 main 分支最新提交是 `aff3880`
- 点击 `pre` 目录，确认文件存在

### 3. 对比文件内容
- 在 GitHub 上打开 `index.html`
- 搜索 `preBtn`，确认代码存在
- 如果 GitHub 上没有，说明 push 失败

---

## 五、重新部署

如果以上方法都无效，可以尝试重新部署：

### Cloudflare Workers/Pages 重新部署
1. 在 Workers & Pages 项目详情页
2. 找到 **Settings** → **Builds & deployments**
3. 点击 **Retry deployment** 或 **Redeploy**
4. 或者推送一个空提交触发重新部署：
   ```bash
   cd /Users/xiaoy/Documents/GitHub/academic-detective-site
   git commit --allow-empty -m "trigger redeploy"
   git push
   ```

### 检查部署日志
1. 在 Workers & Pages 项目详情页
2. 点击最新一次部署
3. 查看 **Build log** 和 **Functions log**
4. 确认是否有报错（如文件过大、构建失败等）

---

## 六、常见问题

| 问题 | 原因 | 解决 |
|:---|:---|:---|
| GitHub 上有 pre/，但网站 404 | Cloudflare 缓存 | 清除缓存或重新部署 |
| 首页没有 pre 按钮 | 首页 HTML 被缓存 | 按 URL 清除首页缓存 |
| 密码框不弹出 | JS 文件被缓存 | 清除 `index.html` 和 JS 文件缓存 |
| pre/index.html 能打开但资源 404 | assets 路径问题 | 检查 `pre/assets/` 是否完整上传 |

---

## 七、需要我协助

如果周老师按以上步骤操作后仍有问题，请把以下信息发给我：
1. Cloudflare Workers & Pages 项目详情页的截图
2. 最新一次部署的日志截图
3. 浏览器开发者工具（F12）Network 面板的截图
4. 直接访问 `https://www.academic-detective.top/pre/index.html` 的结果
